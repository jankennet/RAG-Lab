# Optional: only needed for CLI scripts (ingest, benchmark)
# NIM_API_KEY=nvapi-...

# Required for /api/session to encrypt API keys into httpOnly cookies.
# Must be exactly 64 hex chars (= 32 bytes). Do NOT reuse any other secret value.
# Generate with: openssl rand -hex 32
# SESSION_KEY_SECRET=
