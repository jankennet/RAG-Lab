"use client";

// Ranking historically had its own view; the per-dataset pipeline ranking
// table it described never shipped behind the nav — /compare covers the same
// ground. Kept as an alias so bookmarks and links still resolve.
export { default } from "../compare/page";
